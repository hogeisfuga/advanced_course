<div id='comment-errors'></div>
<%= form_with model: [room, room.messages.new], local:false do |f| %>
  <div class="form-group">
    <%= f.text_field :content, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit "送信", class: 'btn btn-success' %>
  </div>
<% end %>
